extends ../application.pug

block content
  .container
    .card.card-block
      .row
        .col-md-4.hidden-md-down
        .col-xs-12.col-md-8
          h2= notice.title
          h5= notice.body
          .row
            .col-xs-3
              form(action="/notices/" + notice.id method="POST")
